using UnityEngine;
using System.Collections;

public class EnterNextScene : MonoBehaviour
{

    // Update is called once per frame
    void Update ()
    {
        if (Input.GetKeyDown (" ")) {

        }
	
    }
}
